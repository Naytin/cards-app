

const initialState = {
}

const resetUserPasswordReducer = (state = initialState, action: any):{}=> {
    switch (action.type) {
        default: {
            return state
        }
    }
}

export default resetUserPasswordReducer